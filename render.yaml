services:
  - type: web
    name: college-wallpaper-generator
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: CFBD_API_KEY
        sync: false
